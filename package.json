{
  "name": "rapor-karakter-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm install --prefix builder/rapor-karakter-builder && npm install --prefix runner/rapor-karakter-runner",
    "dev:builder": "npm run dev --prefix builder/rapor-karakter-builder",
    "dev:runner": "npm run dev --prefix runner/rapor-karakter-runner",
    "dev": "dotenv -e .env -- concurrently -k -n \"BUILDER,RUNNER\" -c \"cyan,magenta\" \"npm run dev:builder\" \"npm run dev:runner\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "dotenv-cli": "^11.0.0"
  }
}
